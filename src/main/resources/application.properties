# =============================================================================
# SPRING BOOT PROFILE CONFIGURATION
# =============================================================================
# Active Profile Configuration - Can be overridden by environment variable
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# Application Configuration
spring.application.name=dynamic-platform

# =============================================================================
# COMMON CONFIGURATION (Shared across all profiles)
# =============================================================================

# Server Configuration
server.port=${SERVER_PORT:8080}

# Database Configuration (Common settings)
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration (Common settings)
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT Configuration
jwt.secret=${JWT_SECRET:3cfa76ef14937c1c0ea519f8fc057a80fcd04a7420f8e8bcd0a7567c272e007b}
jwt.expiration=${JWT_EXPIRATION:86400000}

# OAuth2 Google Configuration
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:your-google-client-id}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET:your-google-client-secret}
spring.security.oauth2.client.registration.google.scope=profile,email
spring.security.oauth2.client.registration.google.redirect-uri={baseUrl}/oauth2/callback/{registrationId}

# Mail Configuration
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:salahsayedatwa@gmail.com}
spring.mail.password=${MAIL_PASSWORD:mfjxkhptngplnump}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=${MAIL_HOST:smtp.gmail.com}
spring.mail.properties.mail.smtp.ssl.protocols=TLSv1.2

# File Upload Configuration
spring.servlet.multipart.max-file-size=${MAX_FILE_SIZE:10MB}
spring.servlet.multipart.max-request-size=${MAX_REQUEST_SIZE:10MB}

# PDF Generation Configuration
# Engine priority: ironpdf (Cross-platform with proper config) -> playwright (Excellent fallback) -> flyingsaucer (Reliable fallback)
pdf.engine=${PDF_ENGINE:auto}

# IronPDF Configuration (Premium, Cross-platform with proper Linux support)
ironpdf.license-key=${IRONPDF_LICENSE_KEY:IRONSUITE.SALAHSAYEDATWA.GMAIL.COM.6053-62A8B2268E-DSR46CQMYQLOQD-GRH3HZVXGNRW-TCI2DXVHNFR3-JY5I5ETTYO6G-DRTB6EIBAEEX-IQDJOLR5DXKG-6QI7AT-TTJMQSIXSHSQUA-DEPLOYMENT.TRIAL-3W44KS.TRIAL.EXPIRES.15.JAN.2026}
ironpdf.enabled=${IRONPDF_ENABLED:true}
ironpdf.timeout=${IRONPDF_TIMEOUT:30000}
ironpdf.chrome-gpu-mode=${IRONPDF_CHROME_GPU_MODE:disabled}

# Linux-specific IronPDF Configuration
ironpdf.linux-chrome-path=${IRONPDF_LINUX_CHROME_PATH:}
ironpdf.headless=${IRONPDF_HEADLESS:true}
ironpdf.no-sandbox=${IRONPDF_NO_SANDBOX:true}

# Playwright Configuration (Cross-platform, excellent Linux support)
playwright.enabled=${PLAYWRIGHT_ENABLED:true}
playwright.timeout=${PLAYWRIGHT_TIMEOUT:30000}
playwright.headless=${PLAYWRIGHT_HEADLESS:true}
playwright.browser-path=${PLAYWRIGHT_BROWSER_PATH:}
playwright.no-sandbox=${PLAYWRIGHT_NO_SANDBOX:true}
playwright.disable-gpu=${PLAYWRIGHT_DISABLE_GPU:true}
playwright.disable-dev-shm=${PLAYWRIGHT_DISABLE_DEV_SHM:true}

# =============================================================================
# SUBSCRIPTION TIER CONFIGURATION
# =============================================================================

# FREE Tier Configuration
subscription.tiers.FREE.max-apps=${SUBSCRIPTION_FREE_MAX_APPS:2}
subscription.tiers.FREE.max-users=${SUBSCRIPTION_FREE_MAX_USERS:2}
subscription.tiers.FREE.max-api-requests=${SUBSCRIPTION_FREE_MAX_API_REQUESTS:1000}
subscription.tiers.FREE.description=${SUBSCRIPTION_FREE_DESCRIPTION:Free tier with basic features}
subscription.tiers.FREE.enabled=${SUBSCRIPTION_FREE_ENABLED:true}

# PRO Tier Configuration
subscription.tiers.PRO.max-apps=${SUBSCRIPTION_PRO_MAX_APPS:10}
subscription.tiers.PRO.max-users=${SUBSCRIPTION_PRO_MAX_USERS:-1}
subscription.tiers.PRO.max-api-requests=${SUBSCRIPTION_PRO_MAX_API_REQUESTS:50000}
subscription.tiers.PRO.description=${SUBSCRIPTION_PRO_DESCRIPTION:Pro tier with advanced features}
subscription.tiers.PRO.enabled=${SUBSCRIPTION_PRO_ENABLED:true}

# TEAM Tier Configuration
subscription.tiers.TEAM.max-apps=${SUBSCRIPTION_TEAM_MAX_APPS:-1}
subscription.tiers.TEAM.max-users=${SUBSCRIPTION_TEAM_MAX_USERS:50}
subscription.tiers.TEAM.max-api-requests=${SUBSCRIPTION_TEAM_MAX_API_REQUESTS:500000}
subscription.tiers.TEAM.description=${SUBSCRIPTION_TEAM_DESCRIPTION:Team tier for organizations}
subscription.tiers.TEAM.enabled=${SUBSCRIPTION_TEAM_ENABLED:true}

# ENTERPRISE Tier Configuration
subscription.tiers.ENTERPRISE.max-apps=${SUBSCRIPTION_ENTERPRISE_MAX_APPS:-1}
subscription.tiers.ENTERPRISE.max-users=${SUBSCRIPTION_ENTERPRISE_MAX_USERS:-1}
subscription.tiers.ENTERPRISE.max-api-requests=${SUBSCRIPTION_ENTERPRISE_MAX_API_REQUESTS:-1}
subscription.tiers.ENTERPRISE.description=${SUBSCRIPTION_ENTERPRISE_DESCRIPTION:Enterprise tier with unlimited features}
subscription.tiers.ENTERPRISE.enabled=${SUBSCRIPTION_ENTERPRISE_ENABLED:true}
